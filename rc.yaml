apiVersion: v1
kind: ReplicationController
metadata:
  name: demo-app-rc
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: my-app
        type: backend
    spec:
      containers:
        - name: nginx-container
          image: nginx    